{% extends 'base.html.twig' %}

{% block body %}
    <h1>Shiftfunctionorganization</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ shiftFunctionOrganization.id }}</td>
            </tr>
            <tr>
                <th>Amount</th>
                <td>{{ shiftFunctionOrganization.amount }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('shiftfunctionorganization_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('shiftfunctionorganization_edit', { 'id': shiftFunctionOrganization.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
