{{ form_start(form, {action:path('job_new', {'access': 'ajax'}), 'attr': {'id':'newJobForm'}}) }}
    {{ form_widget(form) }}
    <input id="submitNewJob" type="submit" value="Create"  onclick="this.value='Creating';this.disabled=true" />
{{ form_end(form) }}
