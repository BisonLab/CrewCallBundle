{% extends 'base.html.twig' %}
{% block title %}Edit {{ organization }}{% endblock %}
{% block body %}
    <h1>Organization edit</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input class="btn-sm btn-dark" type="submit" value="Save" />
    {{ form_end(edit_form) }}
    <a href="{{ path('organization_show', { 'id': organization.id }) }}" class="btn-sm btn-dark">Cancel</a><br>
{% endblock %}
