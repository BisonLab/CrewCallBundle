{{ form_start(form, {action:path('shiftfunction_new', {'access': 'ajax'}), 'attr': {'id':'newShiftFunctionForm'}, 'style': 'horizontal'}) }}
    {{ form_widget(form) }}
    <input id="submitNewShiftFunction" type="submit" value="Add" onclick="this.value='Adding';this.disabled=true" />
{{ form_end(form) }}
