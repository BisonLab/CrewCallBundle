{{ form_start(form, {action:path('shiftfunction_new', {'access': 'ajax'}), 'attr': {'id':'newShiftFunctionForm'}, 'style': 'inline'}}) }}
    {{ form_widget(form) }}
    <input id="submitNewShiftFunction" type="submit" value="Create" />
{{ form_end(form) }}
