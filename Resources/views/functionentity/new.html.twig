{% extends 'base.html.twig' %}

{% block body %}
    <h1>New Function</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('function_index') }}">Cancel</a>
        </li>
    </ul>
{% endblock %}
