{{ form_start(edit_form, {action:path('shiftorganization_edit', {'id': shiftOrganization.id, 'access': 'ajax'}), 'attr': {'id':'editShiftOrganizationForm'}, 'style': 'inline'}) }}
    {{ form_widget(edit_form) }}
    <input type="submit" value="Save" />
{{ form_end(edit_form) }}
