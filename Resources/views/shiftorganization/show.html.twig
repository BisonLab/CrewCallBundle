{% extends 'base.html.twig' %}

{% block body %}
    <h1>Shiftorganization</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ shiftOrganization.id }}</td>
            </tr>
            <tr>
                <th>Amount</th>
                <td>{{ shiftOrganization.amount }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('shiftorganization_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('shiftorganization_edit', { 'id': shiftOrganization.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
