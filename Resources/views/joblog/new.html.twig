{% extends 'base.html.twig' %}

{% block body %}
    <h1>Add Job</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Add" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('job_index') }}">Cancel</a>
        </li>
    </ul>
{% endblock %}
